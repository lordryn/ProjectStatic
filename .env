# Where to publish the HTML files (e.g., your Nginx/Apache root)
PROJECT_OUTPUT_DIR=/var/www/html/mysite

# The base URL of your live site (for the "View" button)
PROJECT_LIVE_URL=http://127.0.0.1:5000

# Secret key for security (required for Flash messages)
FLASK_SECRET_KEY=super-secret-key-change-this